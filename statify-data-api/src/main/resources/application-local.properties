# Database Configuration
spring.datasource.url=${STATIFY_DATA_STORAGE_POSTGRES_LOCAL_JDBC_URL}
spring.datasource.username=${STATIFY_DATA_STORAGE_POSTGRES_USER}
spring.datasource.password=${STATIFY_DATA_STORAGE_POSTGRES_PASSWORD}
# Server
server.port=${STATIFY_DATA_API_PORT}
server.url=${STATIFY_DATA_API_URL}